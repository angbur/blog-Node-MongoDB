<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Blog</title>
  <style>
    .grid-blog {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    }
    .card {
      margin: 1rem;
    }
    </style>
</head>
<body>
  <div class="container mt-4">
   

    <div class="d-flex flex-wrap justify-content-flexend py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
        <h1>👁️‍🗨️ BLOG ARTICLES<h1>
      </a>
      <a href="/articles/new" class="btn btn-primary">+ New Article</a>
    </div>
    <div class="grid-blog">
      <% articles.forEach(article => { %>
        <div class="card mt-4">
          
          <div class="card-body">
            <a href="articles/edit/<%= article.id %>" class="btn btn-success">Edit</a>
            <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
              <button type="submit" class="btn btn-danger">Delete</button>
            </form>
            <h4 class="card-title mt-4"><%= article.title %></h4>
            <div class="card-subtitle text-muted mb-2">
              Created at <%= article.createdAt.toLocaleDateString() %>
            </div>
            <div class="card-text mb-2"><%= article.description %><br/>
            <a href="articles/<%= article.slug %>" class="btn btn-outline-primary mt-4">Read More</a></div>
          </div>
        </div>
      <% }) %>
  </div>
  </div>
</body>
</html>